{
  "frontend/src/index.css": "Global styles with Sky: Children of the Light OKLCH color system (warm amber primary, pale gold accent, deep midnight-blue background, soft indigo secondary), Cinzel/Raleway fonts, glass-card utilities, celestial glow effects, and sky gradient helper.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Sky CotL palette (midnight, amber, gold, indigo color scales), Cinzel display font, Raleway sans font, celestial box-shadow utilities, and keyframe animations for cloud drift, float-up particles, and light ray pulse.",
  "frontend/src/utils/timezones.ts": "Comprehensive IANA timezone list, timezone detection, formatting helpers, and UTC↔nanosecond conversion utilities for the timetable and chat.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interaction; `useGetCallerUserProfile` now uses `retry: 0` with explicit `staleTime`/`gcTime` so a slow or unreachable backend fails fast instead of blocking the UI indefinitely.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-time onboarding modal with Sky CotL frosted-glass styling; now imports `RegisterResult` enum and handles both `success` and `alreadyRegistered` results as valid completions, with display name capped at 30 characters to match backend validation.",
  "frontend/src/components/UserClockDisplay.tsx": "Dual live-clock component updated to render `EditableDisplayName` for the current user's clock card (allowing inline name editing) while keeping the partner's name as static text.",
  "frontend/src/components/AddEditTaskModal.tsx": "Task add/edit modal wired to real backend timetable mutations; includes a recurrence selector UI with three options (One-time, Every weekday Mon–Fri, Every weekend Sat–Sun) styled with Sky CotL frosted-glass theme, pre-populates recurrence when editing an existing entry.",
  "frontend/src/components/TimetableView.tsx": "Dual-column 24-hour timetable with a fixed-height (540px) scrollable grid container; fixes the AddEditTaskModal prop call to pass `userTimezone={myProfile.timeZone}` (a string) instead of the incorrect `myProfile` object prop, resolving the TypeScript error.",
  "frontend/src/components/ChatInterface.tsx": "Real-time chat UI wired to live backend with 3-second polling; renders inline images via ExternalBlob.getDirectURL(), downloadable file links with original fileName, UTC nanosecond timestamp conversion to local timezone, upload progress bar, and 2MB client-side file size enforcement.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header, footer, and now includes a subtle ShieldAlert admin icon button that opens the AdminPanel dialog, plus the AdminPanel component rendered at the bottom.",
  "frontend/src/pages/LandingPage.tsx": "Sky CotL themed landing page simplified to only handle unauthenticated users; removed all `isAuthenticated` checks, clear/re-login logic, and queryClient usage — the component now simply calls `login()` and lets App.tsx handle all post-auth routing decisions.",
  "frontend/src/pages/Dashboard.tsx": "Main authenticated dashboard view; adds a \"Sky Events\" navigation button (with Star icon) placed before the timetable section that switches to the SkyEventsScreen, and removes the fixed min-height constraint on the timetable container since TimetableView now manages its own scroll height.",
  "frontend/src/App.tsx": "Root app component with a 5-second hard timeout on auth initialization — if `isInitializing` doesn't resolve within 5 seconds, `initTimedOut` is set to `true` and the LandingPage is shown immediately; also handles profile fetch errors by treating them as \"unregistered\" and showing ProfileSetupModal.",
  "frontend/src/components/AnimatedSkyBackground.tsx": "New full-viewport animated sky background component with CSS gradient sky, starfield image, drifting cloud layers at different speeds, radial light rays near the horizon, and 40 floating glowing particles using CSS keyframe animations.",
  "frontend/src/hooks/useAmbientMusic.ts": "Singleton ambient music hook with module-level Audio element, sessionStorage persistence, autoplay handling, and audio event listeners (pause/play) to keep globalIsMuted in sync; exposes isMuted state and toggle function shared across all hook instances.",
  "frontend/src/components/MusicToggleButton.tsx": "Fixed-position ambient music toggle button; now uses Volume2 (speaker with waves) when playing and VolumeX when muted for clearer visual feedback, consuming isMuted/toggle from useAmbientMusic.",
  "frontend/src/components/AdminPanel.tsx": "Passcode-protected admin dialog with three states (passcode entry, danger-zone reset confirmation, success), calls useResetSystem mutation with code 161189, clears query cache on success, styled with Sky CotL frosted-glass amber/indigo theme.",
  "frontend/src/components/EditableDisplayName.tsx": "New inline display name editor component that shows the current name with a hover-reveal pencil icon; clicking it reveals an input pre-filled with the current name plus Save/Cancel buttons, calls `useUpdateDisplayName` on save, and shows inline validation errors.",
  "frontend/src/hooks/useSkyEventCountdown.ts": "Pacific Time-anchored Sky CotL event countdown hook with DST-aware binary search for PT midnight; updated offsets to Geyser +5 min, Grandma +30 min, Turtle +40 min, each with 10-minute durations within a 2-hour cycle.",
  "frontend/src/pages/SkyEventsScreen.tsx": "Sky Events screen with live Sky Mean Time and local time clocks, a three-row events table (Grandma, Turtle, Geyser) with live HH:MM:SS countdowns, and a Pacific Time daily schedule grid; updated all event durations from 7 to 10 minutes."
}